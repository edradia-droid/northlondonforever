(function(){'use strict';const db=()=>window.nl4Supabase;const params=new URLSearchParams(location.search);let idx=Number(params.get('match'));if(!Number.isInteger(idx)||idx<0||idx>7)idx=0;const matchday=idx+1;function voterKey(){let key=localStorage.getItem('nl4_fan_voter_key');if(!key){key=(crypto.randomUUID?crypto.randomUUID():'fan-'+Date.now()+'-'+Math.random().toString(36).slice(2));localStorage.setItem('nl4_fan_voter_key',key)}return key}async function fixture(){const client=db();if(!client)throw new Error('Supabase is not available.');const {data,error}=await client.from('fixtures').select('id,status,kickoff_at').eq('season','2026/27').eq('competition','UEFA Champions League').eq('matchday',matchday).maybeSingle();if(error)throw error;if(!data)throw new Error('Champions League fixture is not connected yet.');return data}async function saveScore(){const msg=document.getElementById('scoreMsg');try{msg.textContent='Saving to NL4…';const f=await fixture();const closed=new Date(f.kickoff_at)<=new Date()||['live','fulltime','finished','ft','played'].includes(String(f.status||'').toLowerCase());if(closed)throw new Error('Predictions are closed for this match.');const hs=Number(document.getElementById('homeScore').value),as=Number(document.getElementById('awayScore').value);const {error}=await db().rpc('submit_fan_prediction',{p_fixture_id:f.id,p_voter_key:voterKey(),p_mode:'score',p_formation:null,p_kit:null,p_starters:[],p_substitutes:[],p_home_score:hs,p_away_score:as});if(error)throw error;msg.textContent='Score prediction saved to Admin Fan Predictions ✓';document.getElementById('saveScore').disabled=true}catch(e){msg.textContent=e.message||'Could not save prediction.'}}async function saveLineup(){const msg=document.getElementById('lineupMsg');try{const selects=[...document.querySelectorAll('.slot select')];const names=selects.map(s=>s.value).filter(Boolean);if(names.length!==11||new Set(names).size!==11)return;msg.textContent='Saving to NL4…';const f=await fixture();const closed=new Date(f.kickoff_at)<=new Date()||['live','fulltime','finished','ft','played'].includes(String(f.status||'').toLowerCase());if(closed)throw new Error('Predictions are closed for this match.');const starters=selects.map(s=>({slot:s.closest('.slot')?.dataset.position||s.getAttribute('aria-label')||'',player_name:s.value}));const substitutes=[...document.querySelectorAll('#subs input:checked')].map(x=>({player_name:x.value}));const {error}=await db().rpc('submit_fan_prediction',{p_fixture_id:f.id,p_voter_key:voterKey(),p_mode:'lineup',p_formation:document.getElementById('formation').value,p_kit:document.getElementById('kit').value,p_starters:starters,p_substitutes:substitutes,p_home_score:null,p_away_score:null});if(error)throw error;msg.textContent='Champions League lineup sent to Admin Fan Predictions ✓'}catch(e){msg.textContent=e.message||'Could not save lineup prediction.'}}function hook(){const score=document.getElementById('saveScore'),lineup=document.getElementById('saveLineup');if(!score||!lineup)return;score.addEventListener('click',()=>setTimeout(saveScore,0));lineup.addEventListener('click',()=>setTimeout(saveLineup,0));fixture().then(f=>{const closed=new Date(f.kickoff_at)<=new Date()||['live','fulltime','finished','ft','played'].includes(String(f.status||'').toLowerCase());if(closed){score.disabled=true;lineup.disabled=true;score.title=lineup.title='Predictions closed'}}).catch(console.warn)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',hook);else hook()})();
NL4 MANAGER IMAGE FIX

All 27 manager portrait images are now embedded directly into managers-data.js.
The managers listing page also has its card images embedded directly in managers.html.

This means the portraits no longer depend on the images/managers folder path to render.
The images/managers folder is still included as a backup/source copy.

IMPORTANT: Keep managers-data.js in the same folder as each dedicated manager HTML page.
